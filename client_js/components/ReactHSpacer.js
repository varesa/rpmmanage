var React = require('react')

var HSpacer = React.createClass({
    'render': function() {
        return (
            <div style={{"width": "25px", "height": "1px", "display": "inline-block"}} />
        )
    }
});

module.exports = HSpacer;